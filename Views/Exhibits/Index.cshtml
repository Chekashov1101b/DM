@model DM.Models.ExhibitEraViewModel

@{
    ViewData["Title"] = "Список экспонатов";
}

<h1>Список экспонатов</h1>

<p>
    <a asp-action="Create">Добавить экспонат</a>
</p>
<p>В выпадающем списке, левее строки поиска, вы выбираете интересующую вас эру. False - экспонаты, созданные до нашей эры, Try - созданные во время нашей эры.</p>
<p>В строке поиска вы можете ввести название интересующего вас экспоната. После ввода названия и или выбора эры создания, нажмите кнопку "Поиск"</p>
<form asp-controller="Exhibits" asp-action="Index" method="get">
    <p>
        <select asp-for="ExhibitEra" asp-items="Model.Era">
            <option value="">Обе эры</option>
        </select>

        Название: <input type="text" asp-for="SearchString" />
        <input type="submit" value="Поиск" />
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Exhibits[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Exhibits[0].Century)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Exhibits[0].Era)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Exhibits[0].Place)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Exhibits[0].Cost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Exhibits[0].Discription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Exhibits[0].Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Exhibits) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Century)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Era)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Place)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Редактировать</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Просмотр</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>